//Form for creating and adding a new user to the database

import React from "react";

const CreateUser = () => {
  return (
    <div>
      <h1>a form for creating a new user</h1>
    </div>
  );
}

export default CreateUser;